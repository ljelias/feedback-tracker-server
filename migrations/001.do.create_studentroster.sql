CREATE TABLE studentroster (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  first_name VARCHAR(60) NOT NULL,
  last_name VARCHAR(60) NOT NULL,
  phone VARCHAR(60),
  email VARCHAR(60),
  misc_info VARCHAR(900)
);